import 'package:flutter/material.dart';

import 'artical_slide.dart';

class InforPage extends StatelessWidget {
  const InforPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const ArticalSlide();
  }
}
